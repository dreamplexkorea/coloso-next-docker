# 이어하기 — 2026-09-07 재설계

## 지금 하는 일

콜로소 디자인을 재현하면서 드림플렉스의 상세 커리큘럼 설계·계획·페이지 발행까지 연결하는 제작 시스템으로 전환한다.

## 이번 변경에서 한 것

- master@d11ed904a73e457963eb842b9144141219264e38의 목표 기록·체크리스트·주요 타입·생성 스크립트·상세 렌더러·파일럿 데이터·견적 라우트를 읽었다.
- 기존 15개 목표를 R01~R15로 보존하고 새 제작 기능 R16~R20을 정의했다.
- `docs/PRODUCT_REDESIGN.md`: 문제 진단, 목적, 사용자 흐름, 디자인 계약, 수업 설계, 데이터·화면 대응, 상태·승인·검증, 예시를 작성했다.
- `docs/IMPLEMENTATION_PLAN.md`: P0~P5와 테스트 T01~T25를 작성했다.
- `AGENTS.md`: AI가 다시 같은 양산형·근거 없는 결과를 만들지 않도록 작업 기준을 작성했다.
- `docs/OPEN_WORRIES.md`: 원자료·디자인 기준·운영 조건의 미확인을 정리했다.
- `CLAUDE.md`: 기존 로그를 보존하고 새 문서 안내와 이번 기록을 추가했다.

## 아직 하지 않은 것

- 애플리케이션 코드 수정, 랜덤 실적 생성 코드의 실제 제거/차단.
- 기존 공개 데이터의 전체 사실 검증.
- 관리자, 교육 생성 엔진, 데이터베이스, 작업 큐, 검증기, 발행 시스템 구현.
- 실제 Coloso 브라우저 캡처, 스타일 측정, 시각 비교.
- 로컬 실행, 빌드·단위·E2E·시각 테스트.
- master 병합 및 운영 배포.

공식 Coloso 페이지의 공개 텍스트와 Playwright 시각 비교 공식 문서는 확인했다. 이를 실제 화면 검증 완료로 해석하지 않는다. 컨테이너에서 GitHub clone을 시도했으나 DNS 접근이 실패하여 저장소 연결 도구로 파일을 읽었다.

## 가장 먼저 할 작업

**P0: `generate-products.mjs`의 무작위 실적·검증되지 않은 공통 경력·제공 보장 생성 경로를 보호하고 기존 결과물을 감사한다.**

이후 P1에서 기준 디자인 캡처와 거인의 어깨 파일럿을 확정한다. 전체 CSS 수정이나 모든 프로그램 대량 생성을 먼저 하지 않는다.

## 확인된 주요 문제

- CurriculumLesson은 id/title/duration만 표현한다.
- 생성 스크립트가 여러 직업에 같은 커리큘럼 구조를 넣고 실적을 무작위 생성한다.
- 거인의 어깨에는 300분·5시간·6시간 표기가 함께 있다.
- Curriculum은 모든 레슨에 '실습 포함'을 표시한다.
- 상세페이지에서 RequiredTools는 import되지만 렌더링되지 않는다.
- ai-quote 핸들러에는 영구 저장 없이 접수 성공 응답을 만드는 경로가 있다.
- 과거 100% 일치는 히어로 일부 수정 검증이며 전체 목적 완료가 아니다.

## 다음 작업자가 남길 기록

날짜 / 기준 커밋 / 요구 ID / 한 일 / 왜 했는지 / 실제 검증 / 실패·미확인 / 다음 작업 / 관련 커밋 또는 PR.

미확인 정보는 OPEN_WORRIES에서 관리하고, 결정이 나면 PRODUCT_REDESIGN의 결정 기록과 영향받는 사양을 함께 갱신한다.
